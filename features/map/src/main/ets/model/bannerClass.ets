export class bannerClass {
  id: string = '';
  src: string = '';
  url: string = '';

  constructor(id: string, src: string, url: string) {
    this.id = id;
    this.src = src;
    this.url = url;
  }
}